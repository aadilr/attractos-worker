{
  "name": "attractos-worker",
  "version": "1.0.0",
  "description": "AttractOS Worker - Track AI bot visits to your website",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.22.0"
  },
  "cloudflare": {
    "bindings": {
      "vars": {
        "ATTRACTOS_KEY": {
          "description": "Your AttractOS site key (find it at attractos.com/dashboard/settings)",
          "required": true
        }
      }
    }
  }
}
